@model string

<div class="admin-empty">
    <p class="mb-1"><strong>Belum ada data</strong></p>
    <p class="text-muted mb-0">@(Model ?? "Tidak ada baris yang cocok dengan filter saat ini.")</p>
</div>
